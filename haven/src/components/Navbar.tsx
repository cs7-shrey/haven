import Logo from "@/components/ui/Logo";
const Navbar = () => {
    return (
        <nav className="w-full h-full bg-primary sticky top-0 z-40 p-4">
           <Logo /> 
        </nav>
    )
}
export default Navbar;
